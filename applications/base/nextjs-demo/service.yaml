apiVersion: v1
kind: Service
metadata:
  name: nextjs-demo
  labels:
    app: nextjs-demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: nextjs-demo
